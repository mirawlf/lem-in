Считывая карту, добавляем текущую комнату в t_map->current, которая в свою очередь содержит ссылку
на t_room, где детально описаны все свойства комнаты (ее порядковый номер, координаты, количество связей,
которые будут итерироваться по мере считывания карты). В t_map в случае первой комнаты (##start) указатель на
предыдущую равен NULL, аналогично для последней комнаты (##end) и указателя на следущую. В prev и next
содержатся ссылки с описанием соответствующих комнат.

t_map->linked должен совпадать со значением t_room->links (мб это совсем и не нужно) для t_map->current, который
указывает на какой-то t_room.

t_room->level и t_map->is_valid при считывании можно заполнить нулями (при вызове ft_bzero после malloc). По
мере реализации алгоритма они будут менять значения и исходя из этого будет понятно, по какой ссылке и через какую
комнату стоит пускать муравьев. Но для последней комнаты t_room->level, наверно, стоит приравнять к inf.

t_room->ant показывает, какой именно муравей сейчас находится в данной комнате

В t_ant хранится информация про всех муравьев: порядковый номер для каждого и в какой комнате каждый из них
находится. В дальнейшем это будет необходимо при выводе программы.